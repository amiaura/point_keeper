/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/point_keeper',
  assetPrefix: '/point_keeper/',
};

module.exports = nextConfig;
